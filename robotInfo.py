class RobotInfo:
    robotLocation = []
    ballsCollected = int
    ballsLocation = []
    ballsDelivered = int
    totalBalls = len(ballsLocation)
    ballsRemaining = int(totalBalls - ballsCollected)

